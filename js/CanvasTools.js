export default class CanvasTools {
    constructor(options = {}) {
        const defaults = {
            debug: false,
        };
        this.options = Object.assign(defaults, options);
        this.init();
    }

    init() {
        
    }

    loadListeners() {
        
    }
}
